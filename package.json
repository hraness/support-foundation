{
  "name": "@hraness/support-foundation",
  "version": "0.3.0",
  "description": "Optional product-update and support invitations with human checkout handoffs and shared local preferences.",
  "license": "MIT",
  "type": "module",
  "sideEffects": false,
  "packageManager": "bun@1.3.14",
  "engines": { "node": ">=22" },
  "repository": { "type": "git", "url": "git+https://github.com/hraness/support-foundation.git" },
  "exports": {
    ".": { "types": "./src/index.ts", "import": "./dist/index.js" },
    "./node": { "types": "./src/node.ts", "import": "./dist/node.js" }
  },
  "files": ["src/index.ts", "src/node.ts", "dist", "docs", "README.md", "LICENSE"],
  "scripts": {
    "build": "bun build src/index.ts src/node.ts --target node --format esm --outdir dist",
    "typecheck": "tsc --noEmit",
    "test": "bun test tests",
    "test:package": "bun run scripts/package-smoke.ts",
    "check": "bun run typecheck && bun run test && bun run build && bun run test:package"
  },
  "devDependencies": {
    "@types/bun": "1.3.14",
    "@types/node": "24.10.1",
    "typescript": "6.0.3"
  }
}
